# HIL Test Harness Dependencies
#
# Also requires idf.py + esptool.py on PATH (source an ESP-IDF environment:
# `. $IDF_PATH/export.sh`) for building/flashing firmware and show bundles --
# those ship with ESP-IDF itself, not via this file.

pytest>=7.0
pyserial>=3.5
websocket-client>=1.5

# Optional but recommended for better output
pytest-timeout>=2.1
pytest-html>=3.1
